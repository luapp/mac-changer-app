{currentAllWifiInterfaces !== undefined && Object.entries(currentAllWifiInterfaces).length > 0 ? (
    <p>{currentAllWifiInterfaces[0][1]}</p>
) : (
    <h1>Loading...</h1>
)}
<br />
{Object.entries(currentAllNetworkInterfaces).length > 0 ? (
    Object.entries(currentAllNetworkInterfaces).map(([key, value], index) => (
        <div key={index}>
            <p>{`Interface Name: ${value[0]}`}</p>
            <p>{`ref: ${value[1]}`}</p>
            <p>{`mac: ${value[2]}`}</p>
        </div>
    ))
) : (
    <h1>Loading...</h1>
)}